# M2 Status — Architect

## Week 0 — Schema Authority

### Completed
- `docs/contracts.md` v1 published. All 7 schemas formalized (IngestedDocument, CurriculumPlan, Script, SceneSpec, GeneratedScene, RenderedScene, AudioOutput/TTSProvider, FinalVideo).
- `workers/lib/schemas.py` Pydantic v2 mirror with `model_config = ConfigDict(extra="forbid")` for strict validation.
- `packages/shared/schemas.ts` TS mirror (hand-written for v1; codegen pipeline deferred).
- `docs/decisions.md` ADRs 001–007 (added 007 for 3b1b RAG fair-use position).
- Established schema change process (RFC PR + required reviewer).

### Outstanding
- TS-from-Pydantic codegen pipeline (`pnpm run codegen:schemas`) — deferred to Week 4 when schemas churn settles.

## Week 1 — Integration Tests

### Completed
- Validated all stage stubs reference schemas correctly (no drift between Pydantic and TS).
- Reviewed cross-stage I/O for consistency. Found and fixed one issue: `Element` discriminated union needed `Field(discriminator="type")` for proper Pydantic dispatch.

### Outstanding
- Stand up `tests/integration/test_full_pipeline.py` once the first hand-written SceneSpec renders successfully (Week 2 Friday).
- Daily green-build report once CI is wired to real Supabase/Modal in Week 2.

## Blockers
None. Schema work is unblocked by anything external.

## Open architectural questions for Week 2
- Should `SceneSpec.elements` enforce that every `target_id` in `timeline` references an existing element? Currently validated only at code-gen prompt level. Lean toward yes — add `model_validator(mode="after")` once we have golden specs to test against.
