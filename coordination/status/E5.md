# E5 Status

## Week 0
No tasks assigned (Week 0 is M1/M2 only).

## Week 1
See coordination/week_1_kickoff.md for full task list. Code-level scaffolding completed by the Claude orchestration seat during Phase 1+2 build (no separate agent spawned for boilerplate efficiency).

### Completed
(populated below as real agent dispatch begins from Week 2 onward)

### Blockers
None on my end — outstanding validation work requires user-provided cloud credentials.

## Week 2 — prompt v2 pass (pre-PDF tightening)

### Files modified
- `workers/prompts/style_guide.md` (in place, no version bump) — appended a "Real 3b1b examples" section with three verbatim passages (Eigenvectors, Fourier series, Inverse matrices) pulled from 3blue1brown.com via WebFetch.
- `workers/prompts/curriculum_v2.md` (new)
- `workers/prompts/script_v2.md` (new)
- `workers/prompts/scene_spec_v2.md` (new)
- `workers/prompts/codegen_v2.md` (new)
- `workers/prompts/codegen_repair_v2.md` (new)
- `scripts/seed_prompts.py` — flipped curriculum/script/scene_spec/codegen/codegen_repair from v1 to v2 (style_guide stays v1).

### What changed per prompt and why

- **curriculum_v2**: Added a full worked example (synthetic 6-page gradient-descent PDF -> CurriculumPlan JSON) so the model sees a concrete shape, not just a schema. Clarified the binding between `concept_dag[*].concept_id` and `scene_outline[*].concept_id` (the v1 prompt mentioned it once in passing). Tightened the anti-patterns list (added "topic-as-objective," "forward prerequisite reference," "duplicate hook framing").
- **script_v2**: Added three contrastive good/bad hook examples (eigenvectors, gradient descent, Fourier series). Made the "no meta-narration" rule operational with a banned-phrase list ("in this video," "let me show you," "first we'll do X then Y"). Added an explicit "equation handling" section that says when to describe vs read aloud, with the rule "never read every symbol of a multi-symbol equation."
- **scene_spec_v2**: Added a full worked ScriptScene -> SceneSpec example (eigenvectors hook) with a 10-action timeline. Tightened the timeline contract: sum of action durations including Waits must equal `duration_sec` (timeline is contiguous; gaps are Waits). Added a "Common mistakes" section (orphan target_id, missing Create before FadeOut, Wait with target_id, Transform without to_id, LaTeX-in-Text vs prose-in-MathTex).
- **codegen_v2**: Added three worked examples (Text-only, Axes+Graph+Indicate, Transform between MathTex) so the model has end-to-end SceneSpec->Python references. Bolded "NO markdown fences, NO commentary, just Python." Added explicit import patterns and a "Text for prose, MathTex for math, never mix" rule.
- **codegen_repair_v2**: Replaced the loose error-class paragraph with a per-class checklist (SyntaxError, ImportError/NameError, LaTeXError, ManimError, TimeoutError, AST-safety failure, missing-class). Each entry has a likely cause and a concrete fix. Added the ManimGL->ManimCE symbol mapping table (`TextMobject`->`Text`, `ShowCreation`->`Create`, etc.).

### Sample new few-shot (script_v2, "Hook B — gradient descent")

> Bad:
> "Gradient descent is an important optimization algorithm in machine learning. The update rule is x sub k plus one equals x sub k minus eta times the gradient of f. We'll now explore why it works."
>
> Why bad: importance claim is unsupported, the equation is read symbol-by-symbol, and "we'll now explore" is a meta-cue.
>
> Good:
> "Imagine you're standing on a foggy hillside. You can't see the valley, but you can feel which way the ground tilts under your feet. So you take a small step downhill, and then another. That is the whole idea, and the only thing we need to make precise is the word 'downhill'."
>
> Why good: the scene's central abstraction (gradient) is introduced by metaphor first; the formal name is teed up without being demanded yet.

### Notes
- No schemas or code logic touched. Changes live in `workers/prompts/` and `scripts/seed_prompts.py` only.
- Persistence comment added at the bottom of every new v2 prompt; the seed script will pick them up on next deploy.
- No emojis added anywhere. The v1 style guide had a few decorative check/cross emojis which I left in place since the task said not to change emoji usage in already-existing prose; only the new sections in v2 files are emoji-free.

### Blockers
None.
