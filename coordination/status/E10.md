# E10 Status

## Week 0
No tasks assigned (Week 0 is M1/M2 only).

## Week 1
See coordination/week_1_kickoff.md for full task list. Code-level scaffolding completed by the Claude orchestration seat during Phase 1+2 build (no separate agent spawned for boilerplate efficiency).

### Completed
(populated below as real agent dispatch begins from Week 2 onward)

### Blockers
None on my end — outstanding validation work requires user-provided cloud credentials.

## Test suite expansion (2026-05-12)

### Files added
- `tests/integration/test_schemas_malformed.py` — malformed-input tests for every top-level schema (extra fields, missing required, ge/le bounds, page_count cap, color pattern, etc.)
- `tests/integration/test_ast_safety_advanced.py` — sandbox-escape attempts (`globals()["os"]`, `__builtins__.eval`, `getattr(__import__('os'), 'system')`, `[].__class__.__base__.__subclasses__()`, ctypes/pickle/socket imports), multi-statement mixed code, legal Manim positives (MathTex/Axes/Transform/Indicate, numpy/math, multi-class modules, typing/functools/itertools).
- `workers/tests/test_pipeline_state.py` — `Pipeline.run()` state-machine: happy path (status reaches `DONE`, `COMPLETED` event fires), `IngestError` -> `FAILED` + error event tagged `stage="ingest"`, unhandled exception -> `FAILED` + payload `kind="<ClassName>"` + re-raise, context-build failure -> `FAILED` + re-raise. Self-contained: `sys.modules["modal"]` / `sys.modules["supabase"]` stubbed at top of file before pipeline import.

### Files modified
- `tests/integration/test_byok_encryption.py` — added key-rotation tests, cross-user isolation, 1-char / empty / 100KB / unicode plaintexts, random-IV non-determinism check, single-byte corruption test.
- `tests/integration/test_manim_gl_to_ce.py` — added deep-import-path, mixed-legacy, `import manimlib`, ThreeDCamera/MovingCamera/MultiCamera, GREEN_SCREEN, bare-manimlib-in-comment, and docstring-rewrite-limitation tests.
- `coordination/blockers.md` — added B-008 (regex-only translator rewrites inside strings/docstrings) and B-009 (local `supabase` import error preexisting in `workers/tests/test_audio_stub.py`).

### Test counts
- Added **71 tests** total:
  - 36 schemas-malformed
  - 16 AST-advanced
  - 4 pipeline state machine
  - 7 manim_gl_to_ce extensions
  - 8 BYOK extensions
- **94 / 94 pass** when run as `python -m pytest -q tests/ workers/tests/test_pipeline_state.py`
- **0 failing** in new/extended tests
- **Preexisting baseline failure** (not caused by E10): `workers/tests/test_audio_stub.py` cannot be collected because `supabase` package isn't installed locally — see B-009.

### Bug findings (not fixed per scope; filed in blockers.md)
- **B-008** — `workers.corpus.manim_gl_to_ce.translate()` is a flat regex pass and rewrites import statements inside string literals / docstrings. Captured in test `test_import_string_inside_string_literal_is_still_rewritten_known_limitation`.
- **B-009** — `workers/tests/test_audio_stub.py` import chain pulls in `from supabase import Client, create_client`, which fails locally when `supabase` is not installed. Local `pytest` from repo root therefore aborts collection. Workaround for new pipeline-state test uses `sys.modules` pre-stubs.
