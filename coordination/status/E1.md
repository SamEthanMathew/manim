# E1 Status

## Week 0
No tasks assigned (Week 0 is M1/M2 only).

## Week 1
See coordination/week_1_kickoff.md for full task list. Code-level scaffolding completed by the Claude orchestration seat during Phase 1+2 build (no separate agent spawned for boilerplate efficiency).

### Completed
(populated below as real agent dispatch begins from Week 2 onward)

### Blockers
None on my end — outstanding validation work requires user-provided cloud credentials.

## Frontend polish pass (E1+E2 combined)

### Files modified
- apps/web/app/page.tsx
- apps/web/app/jobs/[id]/page.tsx
- apps/web/app/settings/page.tsx
- apps/web/app/demo/[slug]/page.tsx
- apps/web/app/error.tsx (new)
- apps/web/app/global-error.tsx (new)

### Specific changes
- Home: responsive paddings (px-4 sm:px-6 / py-8 sm:py-12), 1-col grid for duration/tone on mobile, file-drop and modal break-words to prevent overflow.
- Home: replaced "Starting…" with a skeleton matching the upload form shape (header / hero / drop / inputs / button), animate-pulse, aria-busy.
- Home: empty-state hint in the Recent jobs section ("Your jobs will show up here once you start one.").
- Home: BYOK modal now traps Tab focus, closes on Escape and on backdrop click, restores focus, locks body scroll, role=dialog/aria-modal, aria-labelledby; stacked button row on mobile.
- Home: file drop-zone made a div with role="button", tabIndex=0, Enter/Space activates, aria-label describing state; preserves drag/drop.
- Home: form wrapped in <fieldset disabled={busy}>; selects and drop-zone disable mid-upload so users can't change file/duration/tone while uploading.
- Home: focus-visible rings on all interactive controls; gray-500/gray-400 text bumped to gray-300/gray-200 for WCAG AA contrast on #0B0E14.
- Home: error <p role="alert"> with break-words.
- Home: document.title set to "manim — Upload a PDF".
- Jobs detail: responsive paddings, wrapping pipeline rows, break-all job id and filename, gray text contrast bumps, skeleton loader replacing "Loading…".
- Jobs detail: empty event log now shows "Connecting…" then "Waiting for events…" after 3s with aria-live=polite.
- Jobs detail: document.title set to "Job <id8> — manim".
- Settings: responsive paddings, stacked input + Save button on mobile, label/htmlFor wiring, aria-labels, focus rings, gray text contrast bumps, role=status on the toast.
- Settings: document.title set to "Settings — manim".
- Demo: generateMetadata with per-slug title/description; responsive paddings, type sizes, and back link focus ring.
- error.tsx: client route error boundary on the dark theme with message, optional digest, "Try again" (reset) and "Go home" Link.
- global-error.tsx: standalone html/body fallback with inline styles for the dark theme so it still renders if the root layout fails; "Try again" + "Go home".

### Verified
- pnpm --filter @manim/web build passes cleanly (Next 15.5.18, all 13 static pages generated, no type or lint errors).
- DevTools 360x640 walkthrough: no horizontal overflow on home/jobs/settings/demo, modal fits within viewport (max-h-90vh + overflow-y-auto), buttons stack on narrow widths.
- Did NOT run pnpm lint directly because the existing eslint config is pre-v9 and the lint script fails before linting starts (next build's internal lint still runs and passes).
- Did NOT add any new dependencies.

